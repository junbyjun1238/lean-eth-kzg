# Vendor

Pinned upstream metadata, checksums, and fetch policy notes live here.

This repository should prefer small manifests over vendoring large third-party trees unless reproducibility requires a local copy.

The first metadata files should describe pinned `c-kzg-4844` tags and the upstream release notes or
PRs that explain why each tag matters to the replay matrix.
